{
  "name": "customer-backend-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Reference backend for the GitHub bootstrap and CI/CD PoC.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:coverage": "c8 --reporter=text --reporter=lcov node --test"
  },
  "devDependencies": {
    "c8": "^11.0.0"
  }
}
